@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = null;
}
@using CMS.Entities.Dtos
@model StatusAddDto
<div class="form-group p-4">

    <form asp-action="AddStatus" id="ajaxFormSender" data-url="@Url.Action("AddStatus","Definers")">
        <div asp-validation-summary="All"></div>
        <!-- Name -->
        <div class="row mb-4">
            <label asp-for="Name"></label>
            <input class="form-control" asp-for="Name">
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <!-- Description -->
        <div class="row mb-4">
            <label asp-for="Description"></label>
            <input class="form-control" asp-for="Description">
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <!-- Color -->
        <div class="row mb-4">
            <label asp-for="ColorHex" ></label>
            <input type="color" class="form-control form-control-color" asp-for="ColorHex">
            <span asp-validation-for="ColorHex" class="text-danger"></span>
        </div>

        @*
        <!-- Parent -->
        <div class="row mb-4">
            <label for="status-parent">Durum Ebeveyni</label>
            <select name="parent" id="status-parent" class="form-control">
                <?php
                $parent_query = $connection->get_query("SELECT * FROM site.status");
                while ($parent = $parent_query->fetch_assoc()) :
                if ($_GET['id'] !== $parent['id']) :
                ?>
                <option value="<?echo $parent['id']?>" <?php if ($parent_status == $parent['id']) echo "selected" ?>><?php echo $parent['status'] ?></option>
                <?php
                endif;
                endwhile;
                ?>
            </select>
        </div>
        *@
        
        <!-- Gönder -->
        <div class="row">
            <button type="submit" class="btn btn-primary">Gönder</button>
        </div>

    </form>
    <script src="~/assets/js/custom/ajax-form-sender.js"></script>
</div>
